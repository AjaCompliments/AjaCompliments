// this is where you are going to make 
// a single request that takes all data
// from a guest user where he gives all 
//their information
//and it outputs them one or more compliments
const conn=require('../database/index.js');
module.exports={
   diagnose:(cb,values)=>{
    var output;
    let sql1=``;
    let sql2=``;
    let sql3=``;
     
   }
}